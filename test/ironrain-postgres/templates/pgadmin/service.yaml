---
# Source: ironrain-postgres/templates/pgadmin/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: postgres-pgadmin-svc
  labels:
    run: postgres-pgadmin-svc
spec:
  ports:
  - port: 80
    protocol: TCP
    name: tcp-80
  selector:
    app: postgres-pgadmin-app
